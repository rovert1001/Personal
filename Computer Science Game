import java.util.Scanner;
import java.util.Random;
class Main {
  public static void main(String[] args) {
    
    System.out.println("");
    System.out.println("Hello");
    System.out.println("Welcome to this review game!");
    System.out.println("");
    System.out.println("Type '/help' for help with the game");
    System.out.println("");
    System.out.println("Let's begin");
    System.out.println("");
    
    String definitions = new String [8] {
    	
    	definitions[0] = "A server that provides a specific type of resource to its client, such as just printing.";
    	definitions[1] = "Physical pieces of equipment used to transport data from one computer or peripheral device on the network.";
    	definitions[2] = "A device that is connected to a computer and is controlled by its microprocessor.";
    	definitions[3] = "A network computer that has no local storage and relies on servers for applications, data storage, and administration.";
    	definitions[4] = "Distinct pieces of information, such as files or entries in a database.";
    	definitions[5] = "An array of hard dives that mirror or duplicate other drives. Should the primary hard drive fail, the second drive can be substituted for the failed drive with little or no down time.";
    	definitions[6] = "A server that provides many different services to its client computers, such as file retrieval, printing, and e-mailing.";
    	definitions[7] = "Computers that work behind-the-scenes to provide (serve) shared resources to network users or clients";
    	definitions[8] = "A network computer that requests, or orders, information from a server.";
    	
    }
    
    while (true) {
    
	    Scanner answer = new Scanner(System.in);
	    String answerResponse = answer.nextLine();
	    
	    if (answerResponse.equalsIgnoreCase("/help")) {
	    	
	    	System.out.println("");
	    	System.out.println("GAME RULES");
	    	System.out.println("----------");
	    	System.out.println("");
	    	System.out.println("You will be given a definition and you must type the correct word that fits the definition.");
	    	System.out.println("");
	    	System.out.println("If you want a word list type '/wordlist'");
	    	System.out.println("If a wrong command is typed the game will take it as an incorrect answer.");
	    	System.out.println("");
	    	System.out.println("The game will continue endlessly and terms will be repeated at random.");
	    	System.out.println("Enjoy!");
	    	
	    } else if (answerResponse.equalsIgnoreCase("/wordlist")) {
	    	
	    }
    
    } 
  }
}
